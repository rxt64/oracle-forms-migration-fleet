# 006 - Plan

1. Add typed leg states, JUnit parsing, and a fail-closed report adapter.
2. Emit executable Spring and browser interaction tests for generic and Northstar applications.
3. Run Maven and Vitest through fixed host-owned commands with bounded output and timeouts.
4. Execute generated PostgreSQL DDL in a disposable schema and prove cleanup.
5. Append a separately attested lifecycle phase and require it for production promotion.
6. Persist the report through the existing artifact manifest and expose phase state in the GUI/history.
7. Exercise negative gates locally, then execute both synthetic applications and PostgreSQL 16 in CI.

No model result authorizes a phase or changes a report. The deterministic executor owns dependencies,
mutation authorization, attestation type, and production gating.