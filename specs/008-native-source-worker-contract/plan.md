# 008 - Plan

1. Add a standalone .NET worker executable with a strict `--probe` protocol.
2. Add a hash-pinned host process gateway and unavailable fallback registration.
3. Verify configuration and integrity refusal locally and worker protocol in CI.
4. Add the native provider only after media, matching libraries, x86 host, and genuine FMB are supplied.